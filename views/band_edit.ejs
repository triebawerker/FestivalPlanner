<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>edit band with id: <%= band.id %></p>

    <div class="content">
        <div>
            <form name="band" method="post" action="/band/<%= band.id %>">
                <fieldset>
                    <label>Name</label>
                    <input type="text" name="name" value="<%= band.name %>">
                </fieldset>
                    <input type="submit" value="Save">
            </form>
        </div>
    </div>

  </body>
</html>
