<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>

    <a href="/bands/new">new</a>

    <table>
      <% bands.forEach(function(band){ %>
        <tr>
          <td>name</td>
          <td><%= band.name %></td>
          <td>
            <a href="/bands/edit/<%= band._id %>">edit</a>
          </td>
      <% }); %>
    </ul>


  </body>
</html>
